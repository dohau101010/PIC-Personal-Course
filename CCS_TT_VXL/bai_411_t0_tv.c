   IF (t0_tam != t0)
   {
      t0_tam = t0;
      b411_hienthi_4led7doan ();

      IF (t0 >= g_han)
      {
         set_timer0 (0) ;
         buzzer_on_off (300);
      }
   }
}

   IF (t0_tam != t0)
   {
      t0_tam = t0;
      b411_hienthi_4led7doan ();

      IF (t0 >= g_han)
      {
         set_timer0 (0) ;
         buzzer_on_off (300);
      }
   }
}

